# Wiki 생성 가이드

LangGraph 강의 노트북들을 기반으로 자동으로 지식 그래프(Knowledge Graph)를 생성하는 방법입니다.

## 📋 목차

1. [개요](#개요)
2. [전제 조건](#전제-조건)
3. [빠른 시작](#빠른-시작)
4. [단계별 상세 설명](#단계별-상세-설명)
5. [진행 상황 해석](#진행-상황-해석)
6. [결과 활용](#결과-활용)
7. [문제 해결](#문제-해결)

---

## 개요

### Wiki 시스템이란?

LLM이 모든 노트북을 분석하여 **지식 그래프(Knowledge Graph)**를 자동으로 생성합니다.

- **노트북 페이지**: 각 강의 노트북을 마크다운 문서로 정리
- **개념 페이지**: 각 노트북에서 추출한 핵심 개념을 개별 페이지로 생성
- **위키 링크**: 개념 간의 관계를 `[[개념-이름]]` 형태로 연결
- **인터랙티브 그래프**: D3.js를 사용한 실시간 상호작용형 시각화

### 생성 결과물

```
.rag_cache/wiki/
├── index.md                    # 전체 페이지 목록 (테이블)
├── index.json                  # 빠른 로드용 JSON
├── log.md                      # 생성 작업 로그
├── 01-langgraph-add-memory.md  # 노트북 페이지
├── 01-langgraph-agents.md
├── memorysaver.md              # 개념 페이지
├── thread-id.md
├── state-machine.md
└── ...
```

---

## 전제 조건

### 필수 사항

1. ✅ **PyQt6 앱 실행 중**
   - `python main.py` 로 앱 시작
   
2. ✅ **RAG 시스템 구축 완료**
   - 설정 패널에서 노트북 디렉토리 선택
   - `[🔨 RAG 구축]` 버튼 클릭
   - 진행률 100% 도달 후 완료

3. ✅ **LLM 설정**
   - OpenAI API 키 또는 로컬 LLM 구성
   - `env.txt` 파일에 `OPENAI_API_KEY` 설정

### 선택 사항

- **요약 프롬프트 커스터마이징**: `prompts/summary_prompt.txt` 수정 (없으면 기본값 사용)
- **개념 추출 프롬프트**: `prompts/wiki_concept_prompt.txt` 수정 (없으면 기본값 사용)

---

## 빠른 시작

### 3단계로 Wiki 생성하기

```
1️⃣  RAG 빌드 완료
   ↓
2️⃣  [🗺️ 지식 그래프] 탭 클릭
   ↓
3️⃣  [🔨 Wiki 생성] 버튼 클릭 → 자동 생성 시작
```

**예상 소요 시간**: 3~5분 (노트북 개수와 LLM 응답 속도에 따라 달라짐)

---

## 단계별 상세 설명

### Step 1: RAG 시스템 구축

```
┌─ 앱 시작 ─────────────────────────────┐
│ $ python main.py                      │
└──────────────────────────────────────┘
        ↓
┌─ 좌측 설정 패널 ───────────────────────┐
│ [📂 노트북 디렉토리 선택]               │
│  → `work/` 폴더 선택                   │
│                                        │
│ [🔑 LLM 설정]                         │
│  → OpenAI API 키 입력                 │
│  → 모델명: gpt-4o-mini (기본)          │
│                                        │
│ [🔨 RAG 구축]                         │
│  → 클릭하면 진행률 바 표시              │
│  → 노트북 파싱 + 인덱싱 (~1분)        │
│  → 완료: "✅ 준비 완료 — ..."         │
└──────────────────────────────────────┘
```

### Step 2: Wiki 생성 탭으로 이동

```
┌─ 탭 바 ────────────────────────────────┐
│ 📓 노트북뷰어 | 💬RAG채팅 | 📁디렉토리 │
│ 💾 캐시응답 | 🗺️ 지식 그래프  ←클릭   │
└──────────────────────────────────────┘
        ↓
새 탭이 열리고 아래 메시지 표시:
"💡 RAG 준비 완료 — Wiki 생성 가능"
```

### Step 3: Wiki 생성 시작

```
┌─ 지식 그래프 탭 ──────────────────────┐
│ [🔨 Wiki 생성]  [⏹ 중단(숨김)]        │
│ ☐ 덮어쓰기                            │
│                                        │
│ 상태: 준비 중...                      │
│                                        │
│ [D3.js 그래프 영역]                   │
│ (아직 렌더링 안 됨)                    │
└──────────────────────────────────────┘
        ↓
[🔨 Wiki 생성] 클릭
        ↓
진행 시작!
```

---

## 진행 상황 해석

### Phase별 진행 메시지

#### **Phase 0: 요약 생성 (5→20%)**

```
📝 노트북 요약 자동 생성 중…
요약 생성: 01-LangGraph-Add-Memory
요약 생성: 01-LangGraph-Agents
요약 생성: 01-LangGraph-Building-Graphs
...
```

**의미**: `summaries.json` 파일이 없어서 LLM이 각 노트북의 내용을 분석하고 요약 생성 중

**소요 시간**: 1~2분 (노트북 수 × LLM 응답 시간)

#### **Phase 1: 노트북 페이지 생성 (20→35%)**

```
📑 노트북 페이지 생성 중…
노트북 페이지 생성: 01-LangGraph-Add-Memory
노트북 페이지 생성: 01-LangGraph-Agents
...
```

**의미**: 각 노트북의 요약으로 위키 페이지 생성

**소요 시간**: 수 초 (LLM 없음, 로컬 파일 작성만)

#### **Phase 2: 개념 추출 (35→65%)**

```
🔍 개념 추출 중…
개념 추출: 01-LangGraph-Add-Memory → 6개
개념 추출: 01-LangGraph-Agents → 7개
개념 추출: 01-LangGraph-Building-Graphs → 5개
...
```

**의미**: 각 노트북에서 LLM이 핵심 개념 5~8개 추출

- MemorySaver
- thread_id
- StateGraph
- 등등

**소요 시간**: 1~2분

#### **Phase 3: 개념 페이지 생성 (65→90%)**

```
📝 개념 위키 페이지 생성 중…
개념 페이지: MemorySaver
개념 페이지: thread-id
개념 페이지: state-machine
개념 페이지: checkpoint
...
```

**의미**: 추출된 개념별로 위키 페이지 생성 (최대 40개 개념)

각 개념 페이지는:
- 정의와 설명
- 관련 노트북 참조
- 다른 개념으로의 위키 링크 자동 생성

**소요 시간**: 2~3분

#### **Phase 4: 그래프 빌드 (90→100%)**

```
🗺️ 그래프 JSON 빌드 중…
✅ 완료 — 노드 22개, 엣지 45개
```

**의미**: 모든 위키 페이지를 분석해서 D3.js 그래프 데이터 생성

---

## 결과 활용

### 1️⃣ 인터랙티브 그래프 탐색

생성 완료 후 자동으로 D3.js 그래프가 렌더링됩니다.

```
┌─ 좌측 (60%) ───────────────────┬─ 우측 (40%) ────────────┐
│                                │                        │
│   ◆ D3.js Force Graph          │ # MemorySaver          │
│   ●─→ ● ← 노트북 (파란색)      │                        │
│    ╲  ╱                         │ **Summary**: 체크포인터 │
│     ●  ← 개념 (주황색)         │ 를 사용하여...         │
│      ↓                         │                        │
│   • 드래그로 이동 가능         │ **Sources**:           │
│   • 휠/핀치로 줌               │ [[01-langgraph...]]    │
│   • 노드 클릭 → 상세 표시       │                        │
│                                │ [본문 내용...]         │
│                                │                        │
│                                ├────────────────────────┤
│                                │ 💬 Wiki Q&A            │
│                                │ ┌──────────────────┐  │
│                                │ │ MemorySaver는?   │  │
│                                │ └──────────────────┘  │
│                                │ [전송]                 │
│                                │ LLM 스트리밍 응답...   │
└────────────────────────────────┴────────────────────────┘
```

### 2️⃣ 노드 클릭해서 상세 보기

1. 그래프의 노드 클릭
2. 우측 상단에 해당 페이지 내용 표시
3. 마크다운 형식으로 렌더링

```
# MemorySaver

**Summary**: LangGraph의 체크포인터 구현으로, 대화 상태를 저장하고
복구할 수 있게 합니다.

**Sources**: [[01-langgraph-add-memory]]

**Last updated**: 2026-05-25 18:30:15

---

## 역할

MemorySaver는 conversation history를 저장하고, 동일한 thread_id로
재호출 시 이전 대화를 복구합니다.

## 관련 페이지

- [[thread-id]]
- [[checkpoint]]
- [[state-persistence]]
```

### 3️⃣ Q&A로 개념 설명 요청

우측 하단의 Q&A 패널에서:

1. 질문 입력: "MemorySaver와 thread_id의 차이는?"
2. [전송] 클릭
3. LLM이 위키 내용을 기반으로 실시간 응답

```
입력: "MemorySaver 사용할 때 주의사항은?"

응답:
MemorySaver를 사용할 때 주의할 점:

1. **메모리 누적**: 장기간 대화 후 컨텍스트 윈도우 초과
   → 해결: [[message-trimming]] 또는 요약 사용

2. **프라이버시**: 모든 메시지가 저장됨
   → 민감한 정보는 별도 처리

3. **비용**: 긴 히스토리는 토큰 비용 증가
   → 해결: [[conversation-summarization]] 적용

자세한 내용은 [[01-langgraph-add-memory]] 참고
```

---

## 파일 구조

생성된 Wiki 파일들:

### index.md - 페이지 목록

```markdown
# Wiki Index

전체 페이지 목록 (테이블 형식)

| Name | Type | Slug |
|------|------|------|
| [[01-langgraph-add-memory]] | notebook | 01-langgraph-add-memory |
| [[MemorySaver]] | concept | memorysaver |
| [[thread-id]] | concept | thread-id |
| ...
```

### log.md - 작업 이력

```markdown
# Wiki Log

- **2026-05-25 18:30:15** | notebook_page | 01-LangGraph-Add-Memory
- **2026-05-25 18:30:20** | notebook_page | 01-LangGraph-Agents
- **2026-05-25 18:31:05** | concept_page | MemorySaver
- **2026-05-25 18:31:10** | concept_page | thread-id
- ...
```

### 개념 페이지 예시

파일: `.rag_cache/wiki/memorysaver.md`

```markdown
---
type: concept
slug: memorysaver
label: MemorySaver
---

# MemorySaver

**Summary**: LangGraph의 상태 저장소 구현

**Sources**: [[01-langgraph-add-memory]]

**Last updated**: 2026-05-25 18:31:05

---

## 개요

MemorySaver는 LangGraph에서 대화 상태를 체크포인터로 저장하는
메커니즘입니다. ...

## 사용 예제

```python
from langgraph.checkpoint.memory import MemorySaver
```

## 관련 개념

- [[thread-id]]
- [[state-persistence]]
- [[checkpoint]]
```

---

## 문제 해결

### ❌ "summaries.json을 찾을 수 없습니다"

**원인**: RAG를 구축하지 않음

**해결책**:
1. 좌측 설정 패널에서 노트북 디렉토리 선택
2. `[🔨 RAG 구축]` 버튼 클릭
3. 진행률 100% 완료 대기

### ❌ Wiki 생성 중 LLM 에러

```
❌ Error: 429 Too Many Requests
```

**원인**: OpenAI API 속도 제한 또는 할당량 초과

**해결책**:
1. 잠시 기다렸다가 [🔨 Wiki 생성] 다시 클릭
2. 또는 로컬 LLM 사용 (Ollama 등)
3. `env.txt`에서 LLM 설정 변경

### ❌ 개념 페이지가 너무 적음 (10개 미만)

**원인**: 노트북들이 겹치는 개념이 적거나 추출 실패

**해결책**:
1. `prompts/wiki_concept_prompt.txt` 수정
2. 개념 추출 프롬프트 커스터마이징
3. `[☐ 덮어쓰기]` 체크 후 다시 생성

### ❌ 그래프가 너무 복잡해서 이해 안 됨

**해결책**:
1. D3.js 그래프에서 **노드 드래그**해서 정렬
2. **마우스 휠로 줌아웃** → 전체 구조 파악
3. 특정 노드를 클릭해서 관련 항목만 초점

### ❌ "[⏹ 중단]" 버튼이 안 보임

**설명**: 정상 작동입니다.

- Wiki 생성 중에만 표시됨
- 생성 중 클릭하면 즉시 중단 가능
- 완료 후 자동으로 숨겨짐

---

## 고급 기능

### 📝 Wiki 덮어쓰기

기존 Wiki를 다시 생성하려면:

1. `[☐ 덮어쓰기]` 체크박스 체크
2. `[🔨 Wiki 생성]` 클릭

→ 새로운 프롬프트로 다시 생성됨

### 📂 Wiki 파일 직접 편집

`.rag_cache/wiki/` 폴더의 `.md` 파일을 텍스트 에디터로 열어서:

1. 수동으로 페이지 내용 수정 가능
2. 위키링크 추가/제거
3. 카테고리 재정리

→ 앱 재시작 후 반영됨

### 🔗 프롬프트 커스터마이징

#### 요약 프롬프트 변경

파일: `prompts/summary_prompt.txt`

```
당신은 강의 노트북 내용을 요약하는 전문가입니다.
다음 노트북의 핵심 내용을 5문단 이내로 정리하세요:

[기존 프롬프트 내용...]
```

수정 후 Wiki 생성 시 새로운 프롬프트 적용

#### 개념 추출 프롬프트 변경

파일: `prompts/wiki_concept_prompt.txt`

```
당신은 프로그래밍 강의에서 핵심 개념을 추출합니다.
각 노트북에서 중요한 기술, 라이브러리, 패턴 5~10개를 추출하세요.
```

---

## 참고

### 성능 예상치

| 항목 | 소요 시간 |
|------|---------|
| summaries.json 없을 때 자동 생성 | 1~2분 |
| 노트북 페이지 생성 | 수 초 |
| 개념 추출 (17개 노트북) | 1~2분 |
| 개념 페이지 생성 (40개 개념) | 2~3분 |
| 그래프 JSON 빌드 | 수 초 |
| **총 소요 시간** | **3~7분** |

### API 비용 추정

OpenAI gpt-4o-mini 기준:

```
요약 생성: 17개 노트북 × $0.0001 ≈ $0.002
개념 추출: 17개 노트북 × $0.0001 ≈ $0.002
개념 페이지: 40개 × $0.0002 ≈ $0.008
─────────────────────────────
총 비용 (추정): $0.01 이하
```

---

## 자주 묻는 질문 (FAQ)

**Q: Wiki를 삭제하고 처음부터 다시 생성할 수 있나요?**

A: 네, 두 가지 방법:
1. `.rag_cache/wiki/` 폴더 전체 삭제 후 Wiki 생성
2. `[☐ 덮어쓰기]` 체크 후 생성

**Q: Wiki는 매번 새로 생성해야 하나요?**

A: 아니요, 한 번 생성 후 `.rag_cache/wiki/` 폴더에 저장됨.
앱 재시작해도 유지됨.

**Q: 새 노트북을 추가하면 어떻게 하나요?**

A: 1. RAG 재구축 (새 노트북 인덱싱)
   2. Wiki 생성 (새 노트북 페이지 + 개념 추가)

**Q: 오프라인에서도 Wiki를 볼 수 있나요?**

A: 네, `.rag_cache/wiki/` 파일들은 로컬에 저장되므로
오프라인에서도 그래프 탐색 가능 (Q&A는 LLM 필요)

---

## 문의 및 피드백

- 버그 리포트: [GitHub Issues](https://github.com/anthropics/claude-code/issues)
- 기능 제안: 이 문서의 "고급 기능" 섹션 참고

---

**최종 업데이트**: 2026-05-25
