# ──────────────────────────────────────────────────────────────
# RAG Configuration (config.txt)
# KEY=VALUE 형식, '#'으로 시작하는 줄은 주석
# 이 파일이 없으면 모든 파라미터는 기본값으로 동작합니다.
# ──────────────────────────────────────────────────────────────

# ── Retriever Top-K ──────────────────────────────────────────
# 각 retriever가 반환하는 최대 문서 수
VECTOR_K=5
BM25_K=5
GRAPH_K=5

# ── Graph RAG ────────────────────────────────────────────────
# multi-hop 전파 깊이
GRAPH_HOPS=2
# sequential 엣지 감쇠 계수 (인접 셀 관련성)
SEQ_DECAY=0.5
# shared_var 엣지 감쇠 계수 (변수 공유 관련성)
VAR_DECAY=0.8
# 키워드 보조 점수 최대 부스트 (0~1 범위로 정규화 후 곱하는 계수)
KEYWORD_BOOST=0.4
# Vector seed 문서 수 (Graph RAG 시작점)
SEED_COUNT=3

# ── Ensemble Retriever ───────────────────────────────────────
# Vector + BM25 앙상블 가중치 (합이 1.0이 되어야 함)
VECTOR_WEIGHT=0.6
BM25_WEIGHT=0.4

# ── Merge & LLM ──────────────────────────────────────────────
# 최종 컨텍스트에 포함할 최대 문서 수
MAX_DOCS=10
# LLM 응답 temperature (낮을수록 결정적)
LLM_TEMPERATURE=0.2

# ── Debug / Trace ───────────────────────────────────────────
# true로 설정하면 각 쿼리마다 retriever별 검색 결과를 trace_logs/ 폴더에 저장
TRACE_DEBUG=false

# ── STT (Speech-to-Text) ────────────────────────────────────
# Whisper 모델 크기: small 또는 medium
STT_MODEL=small
